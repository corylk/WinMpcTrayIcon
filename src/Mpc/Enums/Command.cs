namespace WinMpcTrayIcon.Mpc
{
    public enum Command
    {
        stop,
        pause,
        play,
        toggle,
        next,
        prev,
        status,
        clear,
        crop,
        update,
        repeat,
        random,
        single,
        consume,
        outputs,
        search,
        playlist,
        add,
        insert
    }
}
