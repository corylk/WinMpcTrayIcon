namespace WinMpcTrayIcon.Mpc
{
    public enum Command
    {
        stop,
        pause,
        play,
        toggle,
        status,
        clear,
        update,
        repeat,
        random,
        single,
        consume
    }
}
