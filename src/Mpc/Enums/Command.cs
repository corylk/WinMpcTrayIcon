namespace WinMpcTrayIcon.Mpc
{
    public enum Command
    {
        stop = 0,
        pause = 1,
        play = 2,
        toggle = 3,
        status = 4,
        clear = 5,
        repeat = 6,
        random = 7,
        single = 8,
        consume = 9
    }
}
