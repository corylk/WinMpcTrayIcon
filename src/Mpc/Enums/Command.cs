namespace WinMpcTrayIcon.Mpc
{
    public enum Command
    {
        stop = 0,
        pause = 1,
        play = 2,
        toggle = 3,
        status = 4,
        clear = 5,
        update = 6,
        repeat = 7,
        random = 8,
        single = 9,
        consume = 10
    }
}
