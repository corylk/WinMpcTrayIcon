namespace WinMpcTrayIcon.Mpc
{
    public enum Command
    {
        stop,
        pause,
        play,
        toggle,
        next,
        prev,
        status,
        clear,
        update,
        repeat,
        random,
        single,
        consume,
        outputs,
        search,
        add
    }
}
